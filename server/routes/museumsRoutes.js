// import express from "express";
// import { getAllMuseums } from "../controller/museumsController.js";
// const router = express.Router();

// //Эти строки импортируют модуль express и функцию getAllMuseums из файла museumsController.js. Затем создается экземпляр router для маршрутизации.

// router.get("/all", getAllMuseums);
// //Эта строка определяет маршрут для HTTP GET-запроса по пути "/all". Когда на сервер поступает GET-запрос на путь "/all", вызывается функция getAllMuseums из museumsController.js.

// export default router;